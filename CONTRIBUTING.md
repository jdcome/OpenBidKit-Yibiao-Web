# 贡献指南

感谢参与 OpenBidKit 易标 Web 版。

1. 从 `main` 创建独立分支；
2. 不提交无关改动、凭据、客户数据或生成文件；
3. 前端执行 `npm run build`；
4. 后端执行 `pnpm exec prisma generate`；
5. 涉及权限、项目作用域或鉴权时补充管理员与普通用户双向验证；
6. Pull Request 说明修改内容、测试命令和 AGPL/NOTICE 影响。

所有贡献均按 GNU AGPL-3.0-only 提供。
