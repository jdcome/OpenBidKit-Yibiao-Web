# 安全策略

请不要在公开 Issue 中提交 API Key、Token、密码、数据库连接、客户标书、个人信息或可复现生产环境的日志。

发现安全问题时，请通过 GitHub 仓库的 Security Advisories 私下报告。报告中只提供复现所需的最少脱敏信息。

如果秘密曾进入 Git 历史，应立即撤销或轮换该秘密；仅删除工作区文件不足以清除历史暴露。

生产部署必须：

- 使用随机 `JWT_SECRET`；
- 修改初始化管理员密码；
- 限制 PostgreSQL 网络访问；
- 仅通过 HTTPS 暴露 Web 服务；
- 定期备份数据库和运行数据；
- 不将 `.env`、日志或备份提交到源码仓库。
